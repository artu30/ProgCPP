// Devuelve la cadena de indice indicado
const char* getStrForIndex(const char *c[], const int i, const int tableSize);

// Devuelve la cadena de indice indicado, dada la vuelta
char* getRevertStrForIndex(const char *c[], const int i, const int tableSize);

// Devuelve la cadena dada la vuelta
char * reverseString(const char c[]);